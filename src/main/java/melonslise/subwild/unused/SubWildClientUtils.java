/*
package melonslise.expedition.unused;

import net.minecraft.block.BlockState;
import net.minecraft.client.Minecraft;
import net.minecraft.client.renderer.texture.TextureAtlasSprite;
import net.minecraftforge.api.distmarker.Dist;
import net.minecraftforge.api.distmarker.OnlyIn;

@OnlyIn(Dist.CLIENT)
public final class ExpeditionClientUtils
{
	public static final Minecraft MC = Minecraft.getInstance();

	private ExpeditionClientUtils() {}

	public static TextureAtlasSprite particleFromState(BlockState state)
	{
		return MC.getBlockRendererDispatcher().getBlockModelShapes().getTexture(state);
	}
}
*/